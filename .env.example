# Google Cloud Console → Credentials → API key (ograniči na HTTP referrer + YouTube Data API v3)
VITE_YOUTUBE_API_KEY=
# YouTube Studio → Settings → Channel → Advanced → Channel ID (počinje sa "UC")
VITE_YOUTUBE_CHANNEL_ID=
